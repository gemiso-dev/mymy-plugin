---
description: MYMY 브라우저 핸드오프 인증/재인증
---

`mymy_login` 도구를 호출해 브라우저 핸드오프 인증을 실행하세요.
브라우저 창이 열리면 MYMY 웹에 로그인(이미 로그인 상태면 생략) 후 "연결 허용"을 누릅니다.
완료되면 인증 성공 여부와 base_url 을 보고하세요.

> 인증은 사용자 PC 단위로 `~/.mymy-mcp/credentials.json` 에 저장됩니다.
> 토큰 만료(90일)나 관리자 철회로 도구 호출이 401 이 될 때만 다시 실행하면 됩니다.
